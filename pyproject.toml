[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vinci-ai-unseen"
version = "0.1.0"
description = "Deterministic prototype for discovering unseen structure between conversational minds"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
